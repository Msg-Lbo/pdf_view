# 轻阅 · PDF 分组阅读器 — 产品需求文档

## 一、项目概述

| 项 | 说明 |
|---|---|
| **项目名称** | 轻阅 · PDF 分组阅读器 |
| **产品定位** | 专注于「本地 PDF 分组管理 + 沉浸式连续阅读」的轻量工具 |
| **目标平台** | Android（一期）；架构预留 iOS 扩展能力 |
| **语言原则** | 不限制技术栈，优先原生性能与极小安装体积 |

### 核心价值

- 像小说/漫画阅读器一样管理散乱的 PDF：自动分组、自动排序、自动续读
- 将多个 PDF 串联成「章节体」阅读体验，全程竖向滑动，无打断感
- 安装包极小、运行省资源，老旧设备也能流畅使用

---

## 二、目标用户与使用场景

### 用户画像

- 需要阅读大量扫描件、电子书、论文、漫画的学生或职场人
- 习惯把多份 PDF 归类成「一套书」并连续阅读
- 对 APP 体积敏感，反感功能臃肿、广告多的阅读器

### 核心场景

1. 从手机下载目录、微信/QQ 接收目录等位置批量扫描 PDF
2. 将《三体》三部扫描件勾选，生成一个分组，设置封面与标题
3. 打开分组，从上次中断处继续竖滑阅读；滑完第一册末页自动跳转第二册首页
4. 在阅读界面打开目录（分组内 PDF 列表），随时跳转任意章节
5. 完全离线：无需注册、无广告，所有数据存于本地

---

## 三、功能需求

### 3.1 扫描与导入 PDF

| 功能 | 说明 |
|---|---|
| 本地扫描 | 基于 MediaStore / SAF，遍历 Download、Documents、Tencent/QQfile_recv 等常用目录 |
| 多选导入 | 支持按文件夹批量勾选或全选，一键导入应用内「文件库」 |
| 新增监听 | 可选轻量后台服务，监听媒体库变化并提示新 PDF 可导入 |
| 去重 | 基于文件路径与 MD5（部分哈希）自动去除已导入文件 |

### 3.2 分组管理

- **创建**：在文件库中多选 PDF →「新建分组」
- **标题**：可自定义，默认取第一个 PDF 文件名
- **封面**：系统默认图 / 从所选 PDF 提取首页缩略图 / 自定义图片（拍照/相册）
- **排序**：导入后按文件名自然排序（如 `1.pdf, 2.pdf, 10.pdf`）；支持拖拽手动调序
- **维护**：随时增删分组内 PDF、修改标题封面；删除分组不删除原文件
- **展示**：宫格或列表，显示封面、标题、PDF 数量、上次阅读时间

### 3.3 连续阅读器

#### 渲染模式

- 竖向连续滑动：同一纵向滚动容器内排列所有 PDF 页面，无分册断层感
- 逐页加载：仅渲染可见区域 ± 预加载 1–2 页，离开视口即时回收 Bitmap
- 缩放：双指缩放；默认 Fit Width，可切换「适应高度」

#### 自动下一章

- 滚到当前 PDF 最后一页底部时，若分组内还有下一 PDF，显示半透明浮层「下一章：《xxx.pdf》」
- 继续下滑自动切入下一 PDF 首页，或轻上滑触发切换
- 页面索引连续计算（如 A 有 15 页，B 首页为整体第 16 页）；进度条与页码统一逻辑
- 首次触发时轻提示「继续下滑进入下一章」；可在阅读菜单中关闭自动切换

#### 阅读位置记忆

- 退出时保存：分组 ID、PDF 文件 ID、页码、页面内滚动偏移
- 再次打开该分组直接恢复到记忆位置；分组列表按最近阅读时间排序
- 纯本地存储，不涉及云端

#### 目录视图

- 阅读界面呼出抽屉/底部弹出层，展示分组内按序 PDF 列表（文件名 + 缩略图）
- 点击某 PDF 以该文件首页为起点重组阅读流，并更新记忆位置
- 当前正在阅读的 PDF 在目录中高亮

### 3.4 辅助功能

| 功能 | 说明 |
|---|---|
| 搜索 | 全局搜索 PDF 文件名（文件库 + 分组内） |
| 深色模式 | 阅读背景色/亮度调节，适配 OLED 省电 |
| 手势 | 单击呼出菜单；左右滑动无操作（避免与系统返回冲突）；可选边缘滑动返回上一章 |
| 阅读设置 | 深色模式、手势、自动下一章等选项集中在阅读界面菜单，不设独立「我的」页 |

---

## 四、非功能需求

| 指标 | 目标 | 策略 |
|---|---|---|
| 安装包体积 | Android < 8 MB | 极致裁剪依赖；仅引入 PDF 渲染核心；矢量图标；混淆 + 资源缩减 |
| 冷启动 | < 500 ms | 无闪屏广告；Application 不执行耗时初始化；懒加载 |
| 滚动流畅度 | 60 fps，无明显掉帧 | 离屏渲染；Bitmap 复用池；基于 RecyclerView 的自定义 LayoutManager |
| 内存峰值 | 阅读 50 页大 PDF 增量 < 150 MB | 仅缓存可见 + 预加载页；inBitmap；低分辨率预览 |
| 电量 | 无明显异常耗电 | 纯本地；无网络/定位；后台无频繁唤醒 |

---

## 五、技术选型与架构

### 5.1 开发语言与框架

**推荐：纯原生 Android（Kotlin + Android Jetpack）**

- 无跨平台运行时开销，体积最小，性能最佳，可精细控制内存
- PDF 渲染：`PdfRenderer`（API 21+）+ `Pdfium`（加密 PDF 兼容）
- iOS（未来）：Swift + PDFKit

**不采用跨平台框架**：Flutter / React Native 体积与性能难以满足「越小越好」的硬需求。

### 5.2 架构设计

Clean Architecture + MVVM：

| 层级 | 职责 |
|---|---|
| UI 层 | Fragment + XML（优先 View 体系，控制体积）；底部双 Tab：文件库、分组 |
| Domain 层 | 纯 Kotlin 用例，如 `GetGroupReadingProgress`、`ArrangePdfIntoGroup` |
| Data 层 | Repository，屏蔽数据库与文件系统细节 |

### 5.3 核心组件与库

| 组件 | 选型 |
|---|---|
| PDF 渲染 | PdfRenderer + PdfiumAndroid（加密 PDF 可按需动态下载插件） |
| 数据库 | Room |
| 异步 | Kotlin 协程 + Flow |
| 图像 | Coil（封面缩略图） |
| 文件扫描 | MediaStore + DocumentFile |
| 自然排序 | 自实现，支持混合数字字母（类 Windows 文件名排序） |

### 5.4 数据存储（核心表）

```kotlin
// 文件库表
@Entity data class PdfFile(
    @PrimaryKey val id: String,          // MD5+路径生成
    val fileName: String,
    val filePath: String,
    val pageCount: Int,
    val addedTime: Long
)

// 分组表
@Entity data class PdfGroup(
    @PrimaryKey val groupId: String,
    val title: String,
    val coverPath: String?,              // 自定义封面图片路径
    val createdTime: Long
)

// 分组-文件关联表（含排序权重）
@Entity data class GroupPdfCrossRef(
    @PrimaryKey(autoGenerate = true) val crossId: Long,
    val groupId: String,
    val pdfId: String,
    val sortOrder: Int                   // 用于拖拽排序
)

// 阅读进度表
@Entity data class ReadingProgress(
    @PrimaryKey val groupId: String,     // 按分组记录进度
    val currentPdfId: String,
    val currentPage: Int,
    val pageScrollOffset: Float,         // 页面内竖向偏移比例
    val updateTime: Long
)
```

---

## 六、UI/UX 设计

### 导航结构

底部 **双 Tab**：

1. **文件库** — 扫描、导入、搜索 PDF
2. **分组** — 书架式分组列表，点击进入连续阅读

> 不设「我的」Tab。深色模式、手势、自动下一章等设置并入阅读界面菜单，减少层级与包体积。

### 分组首页

- 类似电子书架：大封面 + 标题，无多余阴影动画

### 阅读器界面

- 全屏沉浸；顶部极小进度条（页码/总页，点击弹出菜单）
- 底部半透明迷你目录按钮
- 系统默认控件样式，不引入第三方 UI 库
- 动效仅保留基础过渡，关闭高消耗共享元素动画

---

## 七、开发计划

| 阶段 | 周期 | 内容 |
|---|---|---|
| 第一阶段 | 4 周 | 项目结构、Room、文件扫描导入；单 PDF 竖向滚动渲染；内存缓存与页面回收 |
| 第二阶段 | 3 周 | 分组 CRUD、封面、自然排序；目录视图与跳转；多 PDF 串联与自动下一章 |
| 第三阶段 | 2 周 | 阅读进度记录与恢复；手势、缩放、深色模式；性能压测与 ANR/泄漏排查 |
| 第四阶段 | 1 周 | 依赖瘦身、混淆、资源压缩；真机兼容测试（Android 7.0–14）；发布准备 |

---

## 八、体积控制策略

- 最小化依赖：除 AndroidX 外尽量少用三方库；优先 `kotlinx.coroutines`
- 语言资源：`resConfigs "zh"` 限制语言包
- ProGuard/R8：`minifyEnabled`、`shrinkResources`，精准保留规则
- 动态下载：Pdfium 作为可选插件（约 2 MB），基础 APK 不含
- 图标：全部 VectorDrawable；封面由用户自定义，系统不内置大图
- 架构：避免循环依赖，按需注入，减少无用代码路径

---

## 九、风险与应对

| 风险 | 应对方案 |
|---|---|
| PdfRenderer 对部分非标准 PDF 渲染异常 | Fallback：尝试 Pdfium；向用户提示出错 |
| 超长分组连续滚动内存溢出 | 预加载页数限制为 3；LRU 回收；必要时主动 GC |
| 原生双平台维护成本高 | 一期只做 Android；业务逻辑与 UI 分离，后续可用 KMM 共享逻辑 |
| 用户对自动下一章逻辑不理解 | 首次触发轻提示；阅读菜单中可关闭自动切换 |
